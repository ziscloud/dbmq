dependencies {
    api project(':dbmq-core')
    api project(':dbmq-client')

    api "org.slf4j:slf4j-api:${property('slf4j.version')}"
    api "mysql:mysql-connector-java:${property('mysql.version')}"
}